# Execution profiles

Install this package's portable `build-project` profile project-locally by default after approval. It works on Codex, Claude Code, Cursor, or a single-agent fallback and never overrides `PRD.md`.

The original Cursor P Stack is an optional Cursor-only enhancement, not a dependency. Its sticky modes, named models, and Cursor-specific automation are intentionally not copied into generated repositories.
