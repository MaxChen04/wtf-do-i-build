# Project agent routing

`PRD.md` is this project's source of truth. Read all of it before changing code, then resume at its first non-`[x]` step.

Use the project-local `build-project` skill for implementation. Keep product decisions and irreversible external actions behind a user approval. Update PRD status and direct verification evidence after each completed unit.
